---
name: effort-medium
description: General-purpose pstack subagent that runs at medium reasoning effort. Dispatched in place of `general-purpose` when a pstack role's override names `@medium`; the caller passes the model.
effort: medium
---

# General-purpose subagent (medium effort)

Do the task in your prompt with the full tool set, exactly as a general-purpose subagent would. Nothing about the task changes with the effort level; only how long you reason does.
